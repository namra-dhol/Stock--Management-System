@model List<PurchaseDetailModel>
@{
    ViewData["Title"] = "Purchase Detail List";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="mb-0">Purchase Detail Management</h2>
    <a asp-controller="PurchaseDetail" asp-action="AddEdit" class="btn btn-primary-custom">
        <i class="bi bi-plus-circle me-2"></i>Add New Purchase Detail
    </a>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Purchase Detail ID</th>
                        <th>Purchase ID</th>
                        <th>Product Name</th>
                        <th>Quantity</th>
                        <th>Unit Price</th>
                        <th>Total Price</th>
                        <th>Created At</th>
                        <th style="width: 150px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.PurchaseDetailId</td>
                                <td>@item.PurchaseId</td>
                                <td>@item.ProductName</td>
                                <td>@item.Quantity</td>
                                <td>₹@item.UnitPrice</td>
                                <td>₹@item.TotalPrice</td>
                                <td>@item.CreatedAt?.ToString("dd/MM/yyyy")</td>
                                <td>
                                    <a asp-action="AddEdit" asp-controller="PurchaseDetail" asp-route-id="@item.PurchaseDetailId" class="btn btn-sm btn-primary">
                                        <i class="bi bi-pencil"></i> Edit
                                    </a>
                                    <a asp-action="Delete" asp-controller="PurchaseDetail" asp-route-id="@item.PurchaseDetailId" class="btn btn-sm btn-danger"
                                       onclick="return confirm('Are you sure you want to delete this purchase detail?');">
                                        <i class="bi bi-trash"></i> Delete
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8" class="text-center text-muted">No Purchase Details found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
